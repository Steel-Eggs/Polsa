# 2026-03-12
- Зафиксированы новые траты в финансовую систему: чеки из "СОЮЗ СВ. ИОАННА ВОИНА" (879.96 ₽) и "Магнит" (239.98 ₽) в категорию Семейные/Питание.
- Написала и запустила легковесный локальный Node.js-сервер (порт 4173) для финансового дашборда (`finance/dashboard/server.js`), добавила в дашборд форму для удобного внесения расходов через веб-интерфейс (данные сразу пишутся в CSV, и скрипт Python пересобирает JSON).
- Антон поручил задачу: сравнить SEO и вёрстку на боевом (`skif-avto.ru`) и тестовом (`a0662112.xsph.ru`) сайтах, но доступ с моего сервера к ним заблокирован по геопозиции.
- Установила VPN (AmneziaWG) на сервер, чтобы Антон мог использовать его в качестве обхода блокировок со стороны своего провайдера. Конфиг сохранен в `/home/polsa/awg0-client-anton.conf` и `~/awg-anton.png`.
- Выяснилось, что для доступа моего сервера к `skif-avto.ru` нужно добавить мой IP (`149.33.11.229`) в белый список на хостинге.
- Пыталась программно зайти в панель ishosting.io, чтобы самостоятельно прописать IP, но скрипты (Playwright) блокировались защитой. Составила Антону текст тикета для техподдержки хостинга с просьбой добавить IP в Whitelist.
